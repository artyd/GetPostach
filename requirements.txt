# --- Pigulkin backend (FastAPI) ---
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
pydantic>=2.9
pydantic-settings>=2.6
anthropic>=0.40
httpx>=0.27
python-multipart>=0.0.12

# File ingestion for chat attachments (Excel / legacy xls / DBF customs dumps).
# CSV/JSON/DOCX are handled with the stdlib. Pure-python, light — fine on slim.
openpyxl>=3.1
xlrd>=2.0
dbfread>=2.0

# MCP servers (procurement + email drafts)
mcp>=2.2
google-api-python-client>=2.140
google-auth>=2.34
google-auth-oauthlib>=1.2

# Postgres driver (production / Docker). Local dev can use the built-in sqlite driver.
psycopg[binary]>=3.2 ; platform_system != "Windows"
